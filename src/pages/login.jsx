import { Flex, Text } from "@chakra-ui/react";

export default function Login(){
    return(
        <Flex height={'100vh'} width={'100%'} justifyContent={'center'} alignItems={'center'}>
            <Text>Login form should come here</Text>
        </Flex>
    )
}